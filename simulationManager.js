// TODO: class that owns Simulation and Renderer
export class SimulationManager {
    //simulation: Simulation;
    constructor(
    //private simulation: Simulation
    ) { }
    static async create(device, outputTextureView, computeToTexturePipeline) {
    }
    update(commandEncoder) {
    }
    renderToTexture(commandEncoder) {
    }
}
